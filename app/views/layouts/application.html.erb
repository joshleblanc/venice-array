<!DOCTYPE html>
<html>
  <head>
    <title><%= content_for(:title) || "Venice Array" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="mobile-web-app-capable" content="yes">
    <meta name="turbo-refresh-method" content="morph">
    <meta name="turbo-refresh-scroll" content="preserve">

    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= yield :head %>

    <%# Enable PWA manifest for installable apps (make sure to enable in config/routes.rb too!) %>
    <%#= tag.link rel: "manifest", href: pwa_manifest_path(format: :json) %>

    <link rel="icon" href="/icon.png" type="image/png">
    <link rel="icon" href="/icon.svg" type="image/svg+xml">
    <link rel="apple-touch-icon" href="/icon.png">

    <%# Includes all stylesheet files in app/assets/stylesheets %>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/lightgallery/2.8.3/css/lightgallery.min.css" integrity="sha512-QMCloGTsG2vNSnHcsxYTapI6pFQNnUP6yNizuLL5Wh3ha6AraI6HrJ3ABBaw6SIUHqlSTPQDs/SydiR98oTeaQ==" crossorigin="anonymous" referrerpolicy="no-referrer" />
    <%= stylesheet_link_tag :app, "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body class="min-h-screen">
    <!-- Decorative background elements -->
    <div class="fixed inset-0 overflow-hidden pointer-events-none">
      <div class="absolute top-0 right-0 w-96 h-96 bg-gradient-to-bl from-orange-200/30 to-transparent rounded-full blur-3xl"></div>
      <div class="absolute bottom-0 left-0 w-96 h-96 bg-gradient-to-tr from-amber-200/30 to-transparent rounded-full blur-3xl"></div>
      <div class="absolute top-1/2 left-1/2 transform -translate-x-1/2 -translate-y-1/2 w-64 h-64 bg-gradient-to-r from-pink-200/20 to-orange-200/20 rounded-full blur-2xl"></div>
    </div>

    <header class="fixed top-0 inset-x-0 nav-glass z-50">
      <nav class="container mx-auto px-6 py-4 flex items-center justify-between">
        <div>
          <%= link_to root_path, class: "flex items-center space-x-3 hover:opacity-90 transition-opacity" do %>
            <div class="w-10 h-10 bg-gradient-to-br from-orange-400 to-amber-500 rounded-xl flex items-center justify-center shadow-lg hover:shadow-xl transition-all duration-300 transform hover:-translate-y-0.5">
              <svg class="w-6 h-6 text-white" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 16l4.586-4.586a2 2 0 012.828 0L16 16m-2-2l1.586-1.586a2 2 0 012.828 0L20 14m-6-6h.01M6 20h12a2 2 0 002-2V6a2 2 0 00-2-2H6a2 2 0 00-2 2v12a2 2 0 002 2z"></path>
              </svg>
            </div>
            <span class="heading-font text-2xl font-semibold bg-gradient-to-r from-orange-600 to-amber-600 bg-clip-text text-transparent">Venice Array</span>
          <% end %>
        </div>
        <div class="flex items-center gap-4">
          <% if authenticated? %>
            <span class="text-sm text-gray-600"><%= current_user.balances["USD"] %>$ | <%= current_user.balances["DIEM"] %> DIEM</span>
            <%= link_to "Profile", edit_profile_path, class: "text-gray-600 hover:text-orange-600 font-medium transition-colors duration-200" %>
            <%= button_to "Log out", session_path, method: :delete, class: "btn-secondary" %>
          <% else %>
            <%= link_to "Sign in", new_session_path, class: "text-gray-600 hover:text-orange-600 font-medium transition-colors duration-200" %>
            <%= link_to "Sign up", new_user_path, class: "btn-primary" %>
          <% end %>
        </div>
      </nav>
    </header>

    <main class="relative z-10 container mx-auto mt-32 px-6 pb-12">
      <%= yield %>
    </main>

    <!-- Floating action button for new generation (if authenticated) -->
    <% if authenticated? && request.path == root_path %>
      <div class="fixed bottom-8 right-8 z-40">
        <%= link_to new_generation_array_path, class: "btn-primary rounded-full w-16 h-16 flex items-center justify-center shadow-2xl hover:shadow-3xl" do %>
          <svg class="w-8 h-8" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 4v16m8-8H4"></path>
          </svg>
        <% end %>
      </div>
    <% end %>
  </body>
</html>
